# OS
.DS_Store

# Editor / local tooling
.claude/settings.local.json

# Vercel
.vercel

# Local backend database (SQLite)
vessel_caller.db
vessel_caller.db-*
*.db-journal
*.db-wal
*.db-shm

# Node (not used, but standard hygiene)
node_modules/
*.log

# Python venv + FastAPI backend local db
.venv/
backend/*.db
backend/*.db-*
__pycache__/
*.pyc
.pytest_cache/
