# Dependencies for the Vercel Python serverless function (api/index.py).
# Runtime only — no uvicorn (Vercel provides the ASGI server), no test deps.
fastapi==0.128.8
SQLAlchemy==2.0.51
pydantic==2.13.4
pydantic-settings==2.7.1
PyJWT==2.10.1
passlib==1.7.4
python-multipart==0.0.20
